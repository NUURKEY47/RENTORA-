# Production Environment for Docker
PORT=3000

# Connect to the 'db' service defined in docker-compose.yml
DATABASE_URL="postgresql://postgres:rentorapass@db:5432/rentora_prod?schema=public"

# Production Secret (You should change this on the VPS for real security)
JWT_SECRET="prod_secret_replace_me_with_something_long_and_random"
